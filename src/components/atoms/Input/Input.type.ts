export type InputProps = {
    placeholder?: string;
    value?: string;
    disabled?: boolean;
    error?: string;
    icon?: React.ReactNode;
    iconPosition?: 'left' | 'right' | 'both';
    className?: string;
}